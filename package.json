{
  "name": "deadline-guardian-ai",
  "version": "1.0.0",
  "description": "Multi-Agent AI-powered deadline prevention web application",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "nodemon backend/server.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "diagnose": "npm run diagnose --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
